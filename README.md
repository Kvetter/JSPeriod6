# JSPeriod6

* To add user to the database, start the mongodb and run the file "createUsers" in the "Backend" folder, that should create two users (Per, 123 and Bob, 321)
* The "server secret" is hardcoded but should have been in a file, which should't be uploaded to git
* There are no script to add books to the database, so you should login and the add a book
* You can only add and delete books if you are logged in
* nsp have been used to check the security and a screen shot can be found to see the results
